dist: xenial
sudo: required

language: python
python:
  - "3.5"
  - "3.6"
  - "3.7"

before_install:
  - sudo python3 -m pip install pipenv

install:
  - pipenv install

script:
  - pytest

notifications:
  email: false